{
  "name": "sieve",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for Sieve's optional TensorFlow.js toxicity model (Module 4A, Layer 2). The extension itself is plain JS loaded directly by the manifest; only the optional Layer-2 bundle is produced by esbuild.",
  "scripts": {
    "build": "node build.mjs",
    "build:watch": "node build.mjs --watch",
    "build:chrome": "powershell -ExecutionPolicy Bypass -File ./build-chrome.ps1 -Bundle -Zip",
    "build:firefox": "powershell -ExecutionPolicy Bypass -File ./build-firefox.ps1 -Bundle -Zip",
    "build:all": "npm run build:chrome && npm run build:firefox"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@tensorflow-models/toxicity": "^1.2.2",
    "@tensorflow/tfjs-converter": "^1.7.4",
    "@tensorflow/tfjs-core": "^1.7.4"
  }
}
